package Equals_and_Hashcode_Contrary;

public class theory {
    /*
        how equals() and hashCode() work together :

        equals()  is used to compare two objects for equality

        hashcode() is used in collections like HashMap or HashSet to efficiently
        group and access objects.

        If you override equals(), you must override hashCode() as well to ensure that equal objects have
        the same hash code , otherwise it could lead to unexpected behavior in collection that
        rely on hashing.


     */
}
